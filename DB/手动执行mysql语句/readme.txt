1.首先使用powershell执行外面的create.sql语句
具体操作：a.管理员身份打开powershell
                 b.然后进入到create.sql的目录下
                 c.输入 “g”+"Tab"+"Tab" ----> .\GetSql.ps1   1------>Enter键
                 d.这样就把sql语句复制到本地剪切板了，只需要黏贴到数据库中去执行就ok了

2.在数据库中执行“手动执行mysql语句”中的文件就ok啦